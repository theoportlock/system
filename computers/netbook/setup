##read -p "Continue (y/n)?" choice
#case "$choice" in 
#  y|Y ) echo "yes";;
#  n|N ) echo "no";;
#  * ) echo "invalid";;
#esac#
# copy wifi configuration files
#sudo cp -r wifiprofiles/* /etc/wpa_supplicant/ ;

# install packages
#pacman --needed -S - < programlist ;
#sudo pip install -r piprequirements.txt

# copy rc files
#sudo cp -a rcfiles/. ~/

# install git projects
#cd && 
#git clone https://github.com/theoportlock/thoughts.git &&
#git clone https://github.com/theoportlock/tis.git
#
# change history setup
#shopt -s histappend
#shotp -s cmdhist
#
## change git commit editor
#git config --global core.editor "vim"
#git config diff.tool vimdiff
#
#grive setup
cd ~
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
yay -S grive
mkdir ~/tdrive
cd ~/tdrive
grive -a
mkdir ~/gdrive
grive -a
